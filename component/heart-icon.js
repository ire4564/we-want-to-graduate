import * as React from 'react';
import { Avatar } from 'react-native-paper';

const HeartIcon = () => (
  <Avatar.Image size={60} source={require('../assets/profile-page/heart.png')} />
);
export default HeartIcon;