import * as React from 'react';
import { Avatar } from 'react-native-paper';

const GraphIcon = () => (
  <Avatar.Image size={60} source={require('../assets/profile-page/graph.png')} />
);
export default GraphIcon;